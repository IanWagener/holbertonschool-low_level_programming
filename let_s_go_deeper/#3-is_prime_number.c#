int is_prime_number(int n);

int my_function(int num,int i)
{
  if (num == 1)
    return (0);
  if (num < 0) {
    return(0);
  }
  else{
    if (num % i == 1){
      return 1;
    }
    else
      return (0);
  }
}
);
}
